/*Read two integer value.After that calculate the product between them and store the result in a veriable named PROD.print
the result like the exsmple below,do not forget the end line.
input:the input file contain two integer number
output:print PORD.*/
#include<stdio.h>
int main()
{

  int A,B,PROD;

  scanf("%d %d",&A,&B);
  PROD=A*B;
  printf("PROD = %d\n",PROD);

    return 0;
}
