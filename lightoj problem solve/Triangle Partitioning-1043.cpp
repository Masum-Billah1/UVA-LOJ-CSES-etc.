#include<bits/stdc++.h>
using namespace std;
int main()
{
    int test;
    cin >> test;
    m = test;
    while(test--)
    {
        double ab,ac,bc,r,m,s,ad,ae,de;
        cin >> ab >> ac >> bc >> r;
        s = (ab+ac+bc)/2;
        s = sqrt(s*(s-ab)*(s-ac)*(s-bc));
        as = s/(r+1);
        ad = sqrt(ab*ab/r);
        ac = sqrt(ac*ac/r);

        for(i = 0;i<30;)
        {

        }

    }
}
